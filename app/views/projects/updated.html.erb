<% for project in @projects %>
	<script>
		$('project_<%= project.id -%>').update('<%= (render :partial => "chunk", :locals => { :project => project }).gsub("\n", "") %>');
		new Effect.Highlight('project_<%= project.id -%>');
	</script>
<% end %>